# Label skeleton configuration.
# All sizes are in the unit indicated by the suffix.
# _px values are printer dots (8 dots/mm on the TF P2).

width_mm = 36.0
height_mm = 30.0
dots_per_mm = 8

circle_diameter_mm = 14.5
circle_count = 2
outline_px = 1
horizontal_gap_mm = 0.0

font_path = "C:/Windows/Fonts/verdanab.ttf"

# Font sizes in pixels. 28 px ≈ 10 pt, 56 px ≈ 20 pt at 203 dpi (8 dots/mm).
top_size_px = 28
top_underline = true
middle_size_px = 56
middle_underline = false
bottom_size_px = 28
bottom_underline = true
bottom_default_text = "        "
line_gap_px = 1
